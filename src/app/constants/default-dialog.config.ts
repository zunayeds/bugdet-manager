import { DynamicDialogConfig } from 'primeng/dynamicdialog';

export const DefaultDialogConfig: DynamicDialogConfig<any> = {
  modal: true,
  width: '40rem',
  position: 'center',
  showHeader: false,
};
